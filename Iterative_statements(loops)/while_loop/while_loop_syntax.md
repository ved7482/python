# **Python `while` Loop Syntax**

```python
initialization  # Initialize a variable before the loop starts
while condition:  # The loop will continue to run until the condition evaluates to False
    statement_1
    statement_2
    ...
    statement_n
    increment/decrement  # Update the variable to eventually break the loop
```
